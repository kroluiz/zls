---
name: zls
description: Use when the user asks an agent to work on a ZLS task or provides a ZLS task ID; load the installed ZLS agent contract and task context.
---

Run `zls agent-guide` and follow the returned contract.
Run `zls context <task>` for the selected task before doing task work.
Use ZLS document commands for the durable task record.
